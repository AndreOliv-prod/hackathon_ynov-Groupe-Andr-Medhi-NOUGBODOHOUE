# DEV WEB — Interface de chat

## Contenu
- `app.py` — interface Streamlit connectée à l'API Ollama (`http://localhost:11434`)
- `screenshots/` — interface en fonctionnement : indicateur de connexion, question posée, réponse affichée avec historique

## Résultat
Interface fonctionnelle répondant aux exigences obligatoires : historique de conversation
visible, état de connexion affiché, lancement en une seule commande
(`streamlit run app.py`).
